# oc create role localstorage-mgr --verb='*' --resource=LocalVolumeSets -n openshift-local-storage --dry-run=client -o yaml
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: localstorage-mgr
rules:
- apiGroups:
  - local.storage.openshift.io
  resources:
  - localvolumesets
  verbs:
  - '*'