---
apiVersion: "local.storage.openshift.io/v1alpha1"
kind: LocalVolumeSet
metadata:
  name: local-block
  namespace: openshift-local-storage
spec:
  deviceInclusionSpec:
    minSize: 512Gi
    maxSize: 512Gi 
